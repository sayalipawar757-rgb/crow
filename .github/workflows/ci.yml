name: Deploy
on:
  workflow_dispatch:
   inputs:
    target:
      description: "where to deploy"
      type: choice
      options: [ strating, production]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying to ${{ inputs.target }}"
