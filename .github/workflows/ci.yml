name: CI
on: [push pull]
  depoly:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/crow'
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploiying  cause this is the main"
     
        
