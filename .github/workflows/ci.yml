name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  depoly:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploiying  cause this is the main"
     
        
