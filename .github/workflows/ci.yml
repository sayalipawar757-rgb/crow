name: CI

on:
  push:
  pull_request:

jobs:
  deploy:
  needs: test
  runs-on: ubuntu-latest
  environment: production
  steps:
    - uses: action/checkout@v4
    - name: Deploy
      env:
        TOKEN: ${{ secrets.DEPLOY_TOKEN }}cancel-timeout-minutes: 
      run: echo "Deploying to production"
